// start of pipeline
pipeline {
  // where pipeline job will run
  agent {
    label "windows_slave"
  }
  tools {
  JDK "jdk1.8.0_05"
  maven "MAVEN_HOME"
  }
  stages {
    stage('build')
    {
      steps {
        bat 'mvn clean install'
      }
    }
  }
}
