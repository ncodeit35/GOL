// start of pipeline
pipeline {
  // where pipeline job will run
  agent {
    label "windows_slave"
  }
  tools {
  jdk "JDK"
  maven "maven"
  }
  stages {
    stage('build')
    {
      steps {
        bat 'mvn clean install'
      }
    }
  }
}
