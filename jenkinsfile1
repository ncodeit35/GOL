// start of pipeline
pipeline {
  // where pipeline job will run
  agent {
    label "windows_slave"
  }
  toold {
  jdk "jdk1.8"
  maven "MAVEN_HOME"
  }
  stages {
    stage('build')
    {
      steps {
        bat 'mvn clean install'
      }
    }
  }
}
