// start of pipeline
pipeline {
  // where pipeline job will run
  agent {
    label "windows_slave"
  }
  tools {
  JDK "JAVA_HOME"
  maven "MAVEN_HOME"
  }
  stages {
    stage('build')
    {
      steps {
        bat 'mvn clean install'
      }
    }
  }
}
